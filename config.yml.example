server:
  url: "http://localhost:8081"
  api_key: "dev-api-key-12345"

worker:
  queue: "inference"
  concurrency: 1
  shutdown_timeout: 30s

bootstrap:
  retries: 3
  retry_delay: "5s"

git:
  token: "${GITHUB_PAT}"
  
deployment:
  dir: "./deployment"

retry:
  max_retry: 2
